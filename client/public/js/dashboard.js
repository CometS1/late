function changeTimeDisplayed(timeToDisplay, state){
	state.showing=timeToDisplay;
	console.log(timeToDisplay, state)

}

// Convert the Array of Objects into Dashboard Tasks
function readOutputs(assignments){
	assignments.forEach(function(element){
		/* Example for creating new time block
		var para = document.createElement("p");
		var node = document.createTextNode("This is new.");
		para.appendChild(node);
		var element = document.getElementById("div1");
		element.appendChild(para);
		*/ 
	});
}



